# Profile-specific override, layered on top of src/main/resources/application.properties (not
# a replacement for it) - applied only to @SpringBootTest classes annotated @ActiveProfiles("test").
# See SchedulingConfig.java for why this matters.
scheduling.enabled=false
