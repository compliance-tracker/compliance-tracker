<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/>
	</parent>
	<groupId>com.chrainx</groupId>
	<artifactId>compliance-tracker</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<awssdk.version>2.49.6</awssdk.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>software.amazon.awssdk</groupId>
				<artifactId>bom</artifactId>
				<version>${awssdk.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>sqs</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- OpenAPI spec + Swagger UI (issue #21), generated from the existing controllers/DTOs
		     with no annotations required to get a working baseline - the README's hand-maintained
		     endpoint table drifts out of sync as endpoints grow, this can't. v3.0.x specifically -
		     springdoc-openapi 2.x only supports Spring Boot 3; this project is already on Spring
		     Boot 4.1.0 (Spring Framework 7), and 3.0.x is the first line with real Spring Boot 4
		     support. -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>3.1.0</version>
		</dependency>

		<!-- /actuator/health (issue #44) - a load balancer/container orchestrator's health/
		     readiness check target once this is ever actually deployed (#5). Auto-configures a
		     real DB connectivity health indicator (DataSource is on the classpath via
		     spring-boot-starter-data-jpa below) with zero extra code - readiness genuinely
		     reflects whether the app can reach Postgres, not just that the JVM is up. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<!-- JWT: split into api (compile-time, what our code imports) + impl/gson (runtime only,
		     the actual implementation). jjwt-gson rather than jjwt-jackson deliberately - avoids
		     any interaction with Spring Boot 4.1's Jackson 3 (tools.jackson.*) migration, since
		     jjwt's own JSON serialization is entirely internal and unrelated to our API's JSON. -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.13.0</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-gson</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>

		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- Spring Boot 4 split autoconfiguration into per-technology modules - plain
		     flyway-core alone no longer triggers Flyway's autoconfiguration, since it moved
		     out of the monolithic spring-boot-autoconfigure jar into this dedicated starter. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>

		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- Spring Boot 4 stopped auto-providing TestRestTemplate via @SpringBootTest, and moved
		     it out of spring-boot-starter-test into its own module (also renamed package:
		     org.springframework.boot.resttestclient.TestRestTemplate). Both deps are needed -
		     spring-boot-resttestclient alone doesn't transitively pull in spring-boot-restclient. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-resttestclient</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-restclient</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- In-memory fake SMTP server for tests - lets EmailNotificationSenderIntegrationTest
		     actually send a real message over real SMTP and assert on what arrived, instead of
		     just mocking JavaMailSender and trusting it was called correctly. Never talks to
		     a real mail provider. -->
		<dependency>
			<groupId>com.icegreen</groupId>
			<artifactId>greenmail</artifactId>
			<version>2.1.11</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.icegreen</groupId>
			<artifactId>greenmail-junit5</artifactId>
			<version>2.1.11</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!-- Test coverage reporting (issue #55) - prepare-agent instruments the JVM before
			     any test runs; report generates the actual HTML/XML/CSV output. report's own
			     default lifecycle binding is `verify`, later than plain `mvn test` (what CI's
			     own Test step already runs) ever reaches - bound to `test` explicitly instead
			     (via <phase>, and Maven's within-a-phase plugin execution order, which runs
			     surefire's own `test` binding before this one since it's declared first) so a
			     single `mvn test` run produces the report, not requiring a second `mvn verify`
			     CI never otherwise needed. Deliberately no coverage threshold/gate here - the
			     issue itself frames this as "a useful signal, not a blocker," and picking a real
			     minimum percentage is its own separate decision, not implied by just wanting the
			     number visible. -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.13</version>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>